var valid = true;
for(var i = 1; i <= string_length(argument0); i++)
{
    var character = string_char_at(argument0, i);
    if(string_digits(character) != character)
    {
        
    }
}
return truel;
